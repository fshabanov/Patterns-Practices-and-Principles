const buttonEl = document.getElementById("quit-room-btn");
const readyButtonEl = document.getElementById("ready-btn");
const textContainerEl = document.getElementById("text-container");
const secondsEl = document.getElementById("game-timer-seconds");
const gameTimerEl = document.getElementById("game-timer");
const accuracyEl = document.getElementById("accuracy");

export {
	buttonEl,
	readyButtonEl,
	textContainerEl,
	secondsEl,
	gameTimerEl,
	accuracyEl,
};
